# 🚀 راهنمای قدم‌به‌قدم: گذاشتن سایت روی هاست رایگان

روش پیشنهادی: **Render** (اجرای رایگانِ Node) + **Neon** (دیتابیس رایگان Postgres).
این ترکیب کاملاً رایگان است، کارت بانکی نمی‌خواهد، و داده‌ها **پایدار** می‌مانند
(با هر ری‌استارت پاک نمی‌شوند). کل کار ۱۰ تا ۱۵ دقیقه است.

> چرا دیتابیس؟ روی هاست‌های رایگانِ Node، دیسک «موقتی» است و فایل `data.json` با هر
> ری‌دیپلوی یا خوابیدن سرویس پاک می‌شود. با Neon، داده‌ها بیرون از سرور و سالم می‌مانند.
> کد جوری نوشته شده که اگر متغیر `DATABASE_URL` ست شود، خودکار از Postgres استفاده می‌کند.

---

## مرحلهٔ ۰ — آماده‌سازی
- فایل zip را اکسترکت کن. داخلش پوشهٔ `wc2026` با این فایل‌هاست: `server.js`، `package.json`، پوشهٔ `public/` و…
- یک حساب رایگان **GitHub** بساز (اگر نداری): <https://github.com> — (اگر GitLab را ترجیح می‌دهی هم می‌شود؛ Render هر دو را پشتیبانی می‌کند.)

---

## مرحلهٔ ۱ — کد را در یک ریپوی گیت بگذار

**نکتهٔ مهم:** فایل‌ها باید در «ریشهٔ» ریپو باشند، یعنی `package.json` و `server.js` مستقیماً
در ریشه باشند (نه داخل یک پوشهٔ تو‌در‌تو). اگر پوشهٔ `wc2026` را نگه داشتی، در مرحلهٔ ۳
گزینهٔ Root Directory را روی `wc2026` بگذار.

### راه ساده (بدون خط فرمان)
1. در گیت‌هاب: دکمهٔ **New repository** → یک اسم بده (مثلاً `wc2026`) → **Create**.
2. در صفحهٔ ریپو: **Add file → Upload files** → محتویات داخل پوشهٔ `wc2026` را بکش و رها کن
   (پوشهٔ `public` هم با خودش می‌آید) → **Commit changes**.

### راه خط فرمان (اگر git داری)
```bash
cd wc2026
git init
git add .
git commit -m "WC2026 predictor"
git branch -M main
git remote add origin https://github.com/USERNAME/wc2026.git
git push -u origin main
```
(پوشهٔ `node_modules` و `data.json` با `.gitignore` خودکار نادیده گرفته می‌شوند.)

---

## مرحلهٔ ۲ — دیتابیس رایگان Neon بساز

1. برو به <https://neon.tech> و با همان حساب GitHub وارد شو (Sign up → رایگان).
2. **Create project** → یک اسم بده. **Region** را نزدیک خودت انتخاب کن (مثلاً
   *Europe (Frankfurt)* برای آلمان) تا سرعت بهتر باشد.
3. بعد از ساخت، یک کادر **Connection string** نشان می‌دهد. آن را کامل کپی کن. چیزی شبیه این است:
   ```
   postgresql://user:password@ep-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```
   این رشته را برای مرحلهٔ بعد نگه دار. (اگر گزینهٔ *Pooled connection* بود، همان را بردار.)

---

## مرحلهٔ ۳ — سرویس وب در Render بساز

1. برو به <https://render.com> و با GitHub وارد شو (رایگان).
2. **New +  → Web Service**.
3. ریپوی `wc2026` را وصل/انتخاب کن (اگر اولین بار است، دسترسی GitHub را تأیید کن).
4. تنظیمات:
   - **Language / Runtime:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Instance Type:** **Free**
   - (اگر پوشهٔ `wc2026` را در ریپو نگه داشتی: **Root Directory** را `wc2026` بگذار.)
5. بخش **Environment Variables** → **Add Environment Variable**:
   - **Key:** `DATABASE_URL`
   - **Value:** همان رشتهٔ اتصال Neon از مرحلهٔ ۲.
   - (لازم نیست `PORT` را دستی بدهی؛ Render خودش می‌دهد و کد آن را می‌خواند.)
6. **Create Web Service** را بزن. چند دقیقه صبر کن تا بیلد و دیپلوی تمام شود.
7. در بالای صفحه یک آدرس می‌بینی، مثل:
   ```
   https://wc2026-xxxx.onrender.com
   ```
   در لاگ‌ها باید این خط را ببینی: «ذخیره‌سازی: PostgreSQL (پایدار)».

---

## مرحلهٔ ۴ — تمام! 🎉

- آدرس Render را به دوستانت بده.
- **اولین کسی که وارد شود و اسمش را بزند، «مدیر» می‌شود** و در تب «مدیریت» نتایج واقعی را
  وارد می‌کند. پس بهتر است اول خودت وارد شوی.

---

## ⚠️ دو نکتهٔ مهم دربارهٔ پلن رایگانِ Render

1. **خواب رفتن:** سرویس رایگان بعد از حدود ۱۵ دقیقه بی‌استفاده ماندن «می‌خوابد»؛ اولین
   بازدید بعدی حدود ۳۰ تا ۶۰ ثانیه طول می‌کشد تا بیدار شود. برای یک بازی دوستانه مشکلی نیست.
   - **اختیاری (برای جلوگیری از خواب):** در <https://cron-job.org> (رایگان) یک کرون بساز که
     هر ۱۰ دقیقه آدرس `https://your-app.onrender.com/api/state` را صدا بزند.
2. **داده‌ها در Neon هستند، نه روی Render** — پس ری‌دیپلوی یا خواب/بیداری هیچ داده‌ای را پاک
   نمی‌کند. خیالت راحت.

---

## گزینهٔ جایگزین (سریع‌ترین، ولی داده پاک می‌شود)

اگر فقط می‌خواهی سریع امتحان کنی و برایت مهم نیست که با هر ری‌استارت همه‌چیز صفر شود،
می‌توانی مرحلهٔ ۲ را حذف کنی و متغیر `DATABASE_URL` را **ست نکنی**. آن‌وقت روی فایل ذخیره
می‌کند که روی Render رایگان موقتی است. **برای کل تورنمنت توصیه نمی‌شود.**

## اجرای محلی روی کامپیوتر خودت

```bash
node server.js     # بدون دیتابیس، روی data.json — http://localhost:3000
```
برای تست‌ها: `node test.js` و `npm install` سپس `node test_pg.js`.
